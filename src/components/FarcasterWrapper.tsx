'use client';

import * as React from 'react';

export default function FarcasterWrapper({
  children,
}: {
  children: React.ReactNode;
}): JSX.Element {
  // Placeholder wrapper; integrate Farcaster Mini App SDK here if needed.
  return <>{children}</>;
}
